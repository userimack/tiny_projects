# Script to successfully install packages in a requirements.txt file . When all packages are installed then simply close the scipt by pressing ctrl + z 

while (( 1 ))
do
pip install -r requirements.txt
done
